# # NoteGo (Pet Project)

Простое приложение на Go для работы с заметками:

- REST API (`GET`, `POST`, `DELETE`)
- Сохранение в файл `data.json`
- CLI-команды:
  - `add --title --text`
  - `list`
  - `delete --id`

## Запуск сервера

```bash
go run main.go
